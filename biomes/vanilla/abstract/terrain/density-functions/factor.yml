sampler:
  dimensions: 2
  expression: spline_func(ridges(x, z), erosion(x, z), continents(x, z), ridges_folded(x,
    z))
  functions:
    spline_func:
      arguments: &id001
      - ridges_f
      - erosion_f
      - continents_f
      - ridges_folded_f
      expression: |-
        if(continents_f < -0.19,
        3.95,
        if(continents_f < -0.15,
        spline0(continents_f, -0.19, 3.95, -0.15, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
        if(continents_f < -0.1,
        spline0(continents_f, -0.15, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.1, spline_1(ridges_f, erosion_f, continents_f, ridges_folded_f)),
        if(continents_f < 0.03,
        spline0(continents_f, -0.1, spline_1(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.03, spline_2(ridges_f, erosion_f, continents_f, ridges_folded_f)),
        if(continents_f < 0.06,
        spline0(continents_f, 0.03, spline_2(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.06, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f)),
        spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f))))))
      functions:
        spline: $math/functions/spline.yml:functions.spline
        spline0: $math/functions/spline.yml:functions.spline0
        spline_0:
          arguments: *id001
          expression: |-
            if(erosion_f < -0.6,
            spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f),
            if(erosion_f < -0.5,
            spline0(erosion_f, -0.6, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.5, spline_1(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.35,
            spline0(erosion_f, -0.5, spline_1(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.35, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.25,
            spline0(erosion_f, -0.35, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.25, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.1,
            spline0(erosion_f, -0.25, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.1, spline_2(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.03,
            spline0(erosion_f, -0.1, spline_2(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.03, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.35,
            spline0(erosion_f, 0.03, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.35, 6.25),
            if(erosion_f < 0.45,
            spline0(erosion_f, 0.35, 6.25, 0.45, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.55,
            spline0(erosion_f, 0.45, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.55, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.62,
            spline0(erosion_f, 0.55, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.62, 6.25),
            6.25))))))))))
          functions:
            spline: $math/functions/spline.yml:functions.spline
            spline0: $math/functions/spline.yml:functions.spline0
            spline_0:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.2,
                6.3,
                if(ridges_f < 0.2,
                spline0(ridges_f, -0.2, 6.3, 0.2, 6.25),
                6.25))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_1:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.05,
                6.3,
                if(ridges_f < 0.05,
                spline0(ridges_f, -0.05, 6.3, 0.05, 2.67),
                2.67))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_2:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.05,
                2.67,
                if(ridges_f < 0.05,
                spline0(ridges_f, -0.05, 2.67, 0.05, 6.3),
                6.3))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_3:
              arguments: *id001
              expression: |-
                if(ridges_folded_f < -0.9,
                6.25,
                if(ridges_folded_f < -0.69,
                spline0(ridges_folded_f, -0.9, 6.25, -0.69, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
                spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_0:
                  arguments: *id001
                  expression: |-
                    if(ridges_f < 0,
                    6.25,
                    if(ridges_f < 0.1,
                    spline0(ridges_f, 0, 6.25, 0.1, 0.625),
                    0.625))
                  functions:
                    spline: $math/functions/spline.yml:functions.spline
                    spline0: $math/functions/spline.yml:functions.spline0
                    spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                    spline_right_end: $math/functions/spline.yml:functions.spline_right_end
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_left_end: $math/functions/spline.yml:functions.spline_left_end
            spline_right_end: $math/functions/spline.yml:functions.spline_right_end
        spline_1:
          arguments: *id001
          expression: |-
            if(erosion_f < -0.6,
            spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f),
            if(erosion_f < -0.5,
            spline0(erosion_f, -0.6, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.5, spline_1(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.35,
            spline0(erosion_f, -0.5, spline_1(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.35, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.25,
            spline0(erosion_f, -0.35, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.25, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.1,
            spline0(erosion_f, -0.25, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.1, spline_2(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.03,
            spline0(erosion_f, -0.1, spline_2(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.03, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.35,
            spline0(erosion_f, 0.03, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.35, 5.47),
            if(erosion_f < 0.45,
            spline0(erosion_f, 0.35, 5.47, 0.45, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.55,
            spline0(erosion_f, 0.45, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.55, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.62,
            spline0(erosion_f, 0.55, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.62, 5.47),
            5.47))))))))))
          functions:
            spline: $math/functions/spline.yml:functions.spline
            spline0: $math/functions/spline.yml:functions.spline0
            spline_0:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.2,
                6.3,
                if(ridges_f < 0.2,
                spline0(ridges_f, -0.2, 6.3, 0.2, 5.47),
                5.47))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_1:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.05,
                6.3,
                if(ridges_f < 0.05,
                spline0(ridges_f, -0.05, 6.3, 0.05, 2.67),
                2.67))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_2:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.05,
                2.67,
                if(ridges_f < 0.05,
                spline0(ridges_f, -0.05, 2.67, 0.05, 6.3),
                6.3))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_3:
              arguments: *id001
              expression: |-
                if(ridges_folded_f < -0.9,
                5.47,
                if(ridges_folded_f < -0.69,
                spline0(ridges_folded_f, -0.9, 5.47, -0.69, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
                spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_0:
                  arguments: *id001
                  expression: |-
                    if(ridges_f < 0,
                    5.47,
                    if(ridges_f < 0.1,
                    spline0(ridges_f, 0, 5.47, 0.1, 0.625),
                    0.625))
                  functions:
                    spline: $math/functions/spline.yml:functions.spline
                    spline0: $math/functions/spline.yml:functions.spline0
                    spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                    spline_right_end: $math/functions/spline.yml:functions.spline_right_end
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_left_end: $math/functions/spline.yml:functions.spline_left_end
            spline_right_end: $math/functions/spline.yml:functions.spline_right_end
        spline_2:
          arguments: *id001
          expression: |-
            if(erosion_f < -0.6,
            spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f),
            if(erosion_f < -0.5,
            spline0(erosion_f, -0.6, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.5, spline_1(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.35,
            spline0(erosion_f, -0.5, spline_1(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.35, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.25,
            spline0(erosion_f, -0.35, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.25, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.1,
            spline0(erosion_f, -0.25, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.1, spline_2(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.03,
            spline0(erosion_f, -0.1, spline_2(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.03, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.35,
            spline0(erosion_f, 0.03, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.35, 5.08),
            if(erosion_f < 0.45,
            spline0(erosion_f, 0.35, 5.08, 0.45, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.55,
            spline0(erosion_f, 0.45, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.55, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.62,
            spline0(erosion_f, 0.55, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.62, 5.08),
            5.08))))))))))
          functions:
            spline: $math/functions/spline.yml:functions.spline
            spline0: $math/functions/spline.yml:functions.spline0
            spline_0:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.2,
                6.3,
                if(ridges_f < 0.2,
                spline0(ridges_f, -0.2, 6.3, 0.2, 5.08),
                5.08))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_1:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.05,
                6.3,
                if(ridges_f < 0.05,
                spline0(ridges_f, -0.05, 6.3, 0.05, 2.67),
                2.67))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_2:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.05,
                2.67,
                if(ridges_f < 0.05,
                spline0(ridges_f, -0.05, 2.67, 0.05, 6.3),
                6.3))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_3:
              arguments: *id001
              expression: |-
                if(ridges_folded_f < -0.9,
                5.08,
                if(ridges_folded_f < -0.69,
                spline0(ridges_folded_f, -0.9, 5.08, -0.69, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
                spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_0:
                  arguments: *id001
                  expression: |-
                    if(ridges_f < 0,
                    5.08,
                    if(ridges_f < 0.1,
                    spline0(ridges_f, 0, 5.08, 0.1, 0.625),
                    0.625))
                  functions:
                    spline: $math/functions/spline.yml:functions.spline
                    spline0: $math/functions/spline.yml:functions.spline0
                    spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                    spline_right_end: $math/functions/spline.yml:functions.spline_right_end
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_left_end: $math/functions/spline.yml:functions.spline_left_end
            spline_right_end: $math/functions/spline.yml:functions.spline_right_end
        spline_3:
          arguments: *id001
          expression: |-
            if(erosion_f < -0.6,
            spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f),
            if(erosion_f < -0.5,
            spline0(erosion_f, -0.6, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.5, spline_1(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.35,
            spline0(erosion_f, -0.5, spline_1(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.35, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.25,
            spline0(erosion_f, -0.35, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.25, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < -0.1,
            spline0(erosion_f, -0.25, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.1, spline_2(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.03,
            spline0(erosion_f, -0.1, spline_2(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.03, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.05,
            spline0(erosion_f, 0.03, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.05, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.4,
            spline0(erosion_f, 0.05, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.4, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.45,
            spline0(erosion_f, 0.4, spline_3(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.45, spline_4(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.55,
            spline0(erosion_f, 0.45, spline_4(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.55, spline_4(ridges_f, erosion_f, continents_f, ridges_folded_f)),
            if(erosion_f < 0.58,
            spline0(erosion_f, 0.55, spline_4(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.58, 4.69),
            4.69)))))))))))
          functions:
            spline: $math/functions/spline.yml:functions.spline
            spline0: $math/functions/spline.yml:functions.spline0
            spline_0:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.2,
                6.3,
                if(ridges_f < 0.2,
                spline0(ridges_f, -0.2, 6.3, 0.2, 4.69),
                4.69))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_1:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.05,
                6.3,
                if(ridges_f < 0.05,
                spline0(ridges_f, -0.05, 6.3, 0.05, 2.67),
                2.67))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_2:
              arguments: *id001
              expression: |-
                if(ridges_f < -0.05,
                2.67,
                if(ridges_f < 0.05,
                spline0(ridges_f, -0.05, 2.67, 0.05, 6.3),
                6.3))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_3:
              arguments: *id001
              expression: |-
                if(ridges_folded_f < 0.45,
                spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f),
                if(ridges_folded_f < 0.7,
                spline0(ridges_folded_f, 0.45, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), 0.7, 1.56),
                1.56))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_0:
                  arguments: *id001
                  expression: |-
                    if(ridges_f < -0.2,
                    6.3,
                    if(ridges_f < 0.2,
                    spline0(ridges_f, -0.2, 6.3, 0.2, 4.69),
                    4.69))
                  functions:
                    spline: $math/functions/spline.yml:functions.spline
                    spline0: $math/functions/spline.yml:functions.spline0
                    spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                    spline_right_end: $math/functions/spline.yml:functions.spline_right_end
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_4:
              arguments: *id001
              expression: |-
                if(ridges_folded_f < -0.7,
                spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f),
                if(ridges_folded_f < -0.15,
                spline0(ridges_folded_f, -0.7, spline_0(ridges_f, erosion_f, continents_f, ridges_folded_f), -0.15, 1.37),
                1.37))
              functions:
                spline: $math/functions/spline.yml:functions.spline
                spline0: $math/functions/spline.yml:functions.spline0
                spline_0:
                  arguments: *id001
                  expression: |-
                    if(ridges_f < -0.2,
                    6.3,
                    if(ridges_f < 0.2,
                    spline0(ridges_f, -0.2, 6.3, 0.2, 4.69),
                    4.69))
                  functions:
                    spline: $math/functions/spline.yml:functions.spline
                    spline0: $math/functions/spline.yml:functions.spline0
                    spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                    spline_right_end: $math/functions/spline.yml:functions.spline_right_end
                spline_left_end: $math/functions/spline.yml:functions.spline_left_end
                spline_right_end: $math/functions/spline.yml:functions.spline_right_end
            spline_left_end: $math/functions/spline.yml:functions.spline_left_end
            spline_right_end: $math/functions/spline.yml:functions.spline_right_end
        spline_left_end: $math/functions/spline.yml:functions.spline_left_end
        spline_right_end: $math/functions/spline.yml:functions.spline_right_end
  type: EXPRESSION
